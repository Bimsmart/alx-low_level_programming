#include "main.h"
/**
 * _abs - this function Computes the absolute value of integers
 * @r: this is the integer type
 * Return: Always 0;
 */
int _abs(int r)
{
	if (r < 0)
	{
		return (-r);
	}
	else
	{
		return (r);
	}
}

