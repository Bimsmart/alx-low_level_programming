#include "main.h"
/**
 * _isdigit - this function ckecks for diigits between 0 - 0
 * @c: This is a character
 * Return: 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}


}
