#include "main.h"
#include <string.h>
/**
 * _strlen -  this prints the length of a string
 * @s: the string whose length is to be p be printed
 * Return: int
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
